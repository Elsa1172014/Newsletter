# دليل نشر استوديو النشرة — خطوة بخطوة

لا حاجة لأي خبرة برمجية. اتبع الخطوات بالترتيب. الوقت التقديري: 15-20 دقيقة، والتكلفة صفر (المستويات المجانية كافية تمامًا لهذه المرحلة).

---

## الخطوة 1 — الحصول على مفتاح Gemini API (مجانًا)

1. افتح: https://aistudio.google.com/apikey
2. سجّل الدخول بحساب Google (بريد المدرسة أو أي حساب Google).
3. اضغط **Create API key**.
4. اختر مشروع Google Cloud موجودًا أو دع Google تنشئ واحدًا تلقائيًا.
5. انسخ المفتاح فورًا واحفظه مؤقتًا في مكان آمن (Notes مثلًا) — ستحتاجه في الخطوة 4.

> لا حاجة لبطاقة دفع في هذه الخطوة — المستوى المجاني يكفي تمامًا لتوليد نشرة واحدة أسبوعيًا. الحدود المجانية قابلة للتغيير من Google دون إشعار مسبق، فإن واجهت يومًا رسالة تجاوز حد، انتظر حتى اليوم التالي أو راجع https://ai.google.dev/gemini-api/docs/pricing لأحدث الأرقام.

---

## الخطوة 2 — رفع الملفات إلى GitHub (بدون أوامر برمجية)

1. افتح https://github.com وأنشئ حسابًا مجانيًا إن لم يكن لديك واحد.
2. اضغط الزر الأخضر **New** (أو أيقونة + أعلى الصفحة → New repository).
3. اسم المستودع: `arabic-newsletter-studio` (أو أي اسم تريده). اجعله **Private** إن أردت.
4. اضغط **Create repository**.
5. في الصفحة التالية اضغط **uploading an existing file**.
6. اسحب وأفلت الملفات الثلاثة المرفقة معك هنا:
   - `index.html`
   - `package.json`
   - مجلد `api` بالكامل (يحتوي على `generate.js`)
7. اضغط **Commit changes** أسفل الصفحة.

---

## الخطوة 3 — ربط المستودع بـ Vercel

1. افتح https://vercel.com وسجّل الدخول باستخدام **نفس حساب GitHub** (زر Continue with GitHub).
2. اضغط **Add New... → Project**.
3. ستظهر قائمة بمستودعات GitHub — اختر `arabic-newsletter-studio` واضغط **Import**.
4. اترك كل الإعدادات الافتراضية كما هي (Vercel يتعرف تلقائيًا على أنه مشروع Static + Serverless Function).

---

## الخطوة 4 — إضافة مفتاح Gemini API (الخطوة الأهم أمنيًا)

قبل الضغط على Deploy مباشرة:

1. في نفس صفحة الاستيراد، افتح قسم **Environment Variables**.
2. أضف:
   - **Name**: `GEMINI_API_KEY`
   - **Value**: المفتاح الذي نسخته في الخطوة 1 (يبدأ عادة بـ `AIza...`)
3. اضغط **Add**.
4. الآن اضغط **Deploy**.

انتظر 30-60 ثانية حتى ينتهي البناء.

---

## الخطوة 4.5 — إنشاء قاعدة بيانات لحفظ النشرات (ضرورية — بدونها لن تُحفظ أي نشرة)

> **تنويه:** كانت هذه الخطوة تشير سابقًا إلى "Vercel KV" — لكن Vercel أوقفت هذا المنتج تحديدًا كخدمة مستقلة، والبديل الرسمي الحالي هو **Upstash Redis** عبر Vercel Marketplace (بنفس الفكرة تمامًا: قاعدة بيانات فورية بلا خادم، مجانية للاستخدام الخفيف). الخطوات التالية محدّثة لتطابق الواجهة الحالية.

1. من لوحة مشروعك على Vercel (**وليس** لوحة الحساب العامة)، افتح تبويب **Storage** أعلى الصفحة.
2. اضغط **Create Database**.
3. ستظهر قائمة مزوّدين من الـ Marketplace (Neon، Upstash، Supabase وغيرها). اختر **Upstash**.
4. من خيارات Upstash، اختر منتج **Redis** (وليس Vector أو QStash).
5. اختر خطة **Free** (تكفي تمامًا حجم بيانات نشرة أسبوعية)، واختر المنطقة (Region) الأقرب جغرافيًا — أي منطقة في أوروبا أو آسيا مناسبة لدبي.
6. اضغط **Continue** ثم **Create** لإتمام إنشاء القاعدة.
7. بعد الإنشاء، ستظهر صفحة تفاصيل القاعدة — اضغط **Connect Project** (أو **Connect to Project**).
8. اختر مشروع `arabic-newsletter-studio` من القائمة، وتأكد أن كل البيئات (Production, Preview, Development) مُفعّلة، ثم اضغط **Connect**.
   - هذه الخطوة تضيف متغيرات البيئة اللازمة تلقائيًا (باسم `KV_REST_API_URL` و`KV_REST_API_TOKEN` أو `UPSTASH_REDIS_REST_URL`/`UPSTASH_REDIS_REST_TOKEN` حسب نسخة التكامل) — **لا حاجة لنسخ أو كتابة أي قيمة يدويًا**، الكود في `api/kv.js` يتعرف على كلا التسميتين تلقائيًا.
9. اذهب إلى تبويب **Deployments** أعلى المشروع → اضغط **⋯** بجانب آخر نشر → **Redeploy**. هذه الخطوة إلزامية: المتغيرات الجديدة لا تُطبَّق إلا بعد إعادة نشر.

**كيف تتأكد أن الربط نجح؟** بعد إعادة النشر، افتح الموقع → ادخل كإدارة → ولّد نشرة تجريبية → إن ظهرت لك بنجاح ثم وجدتها لاحقًا في الأرشيف بعد تحديث الصفحة (F5)، فالربط سليم. إن ظهر خطأ يذكر "لم يتم العثور على بيانات اتصال قاعدة البيانات"، فهذا يعني أن الربط أو إعادة النشر لم تكتمل — راجع الخطوتين 8 و9.

---

## الخطوة 5 — التجربة

1. بعد انتهاء النشر، ستظهر شاشة تهنئة مع رابط مثل:
   `https://arabic-newsletter-studio.vercel.app`
2. افتح الرابط.
3. جرّب "إنشاء نشرة جديدة" حتى نهاية النموذج واضغط "توليد النشرة بالذكاء الاصطناعي".
4. هذه المرة الاتصال يمر عبر خادمك الخاص على Vercel (وليس عبر معاينة المتصفح المؤقتة) — ولذلك يجب أن يعمل بثبات كامل.

---

## إن ظهر خطأ

- **"GEMINI_API_KEY غير معرّف"** → عد إلى Vercel → Project Settings → Environment Variables، تأكد من إضافة المفتاح، ثم أعد النشر من تبويب Deployments → زر ⋯ → Redeploy.
- **خطأ "تم تجاوز الحد المجاني" أو ما شابه** → المستوى المجاني له سقف طلبات يومي؛ انتظر حتى اليوم التالي أو راجع https://ai.google.dev/gemini-api/docs/pricing.
- **خطأ "مفتاح غير صالح"** → تأكد أنك نسخت المفتاح كاملًا من aistudio.google.com/apikey دون مسافات إضافية.
- أي خطأ آخر → افتح Vercel → مشروعك → تبويب **Logs** لرؤية تفاصيل الخطأ الفعلي من الخادم.

---

## الخطوة 6 (اختياري) — نطاق مخصص

من Vercel → Project → Settings → Domains، يمكنك ربط نطاق مثل `newsletter.gemsfounders.ae` إن توفر لديكم نطاق فرعي من إدارة تقنية المعلومات بالمدرسة.

---

## ما بعد هذه المرحلة

هذا النشر يمنحك **منصة تعمل فعليًا وبشكل ثابت** بنفس مزايا النموذج الذي جربته، مع اتصال ذكاء اصطناعي حقيقي وموثوق. الأدوار الحقيقية (رئيس قسم / معلم)، قاعدة البيانات الدائمة عبر كل الأجهزة، وتسجيل الدخول عبر حساب المدرسة (Entra ID) — هذه تحتاج الطبقة الموصوفة في مستند "معمارية المنصة الإنتاجية" (Supabase) الذي أرسلته سابقًا، وهي خطوة لاحقة منفصلة يمكن البناء عليها متى قرّرت المضي فيها.
